[project]
name = "synthspark-mcp"
version = "0.1.0"
description = "SynthSpark 博客系统 MCP 服务"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=0.4.0",
    "httpx>=0.25.0",
    "pydantic>=2.0.0",
]
