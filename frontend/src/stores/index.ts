/**
 * Store 统一导出
 * 老大让我导出所有store，我就导出 (´；ω；`)
 * 这活儿简单，但我已经写了太多代码了...
 */

// 主题状态
export { useThemeStore } from './theme'
export type { Theme } from './theme'

// 认证状态
export { useAuthStore } from './auth'
export type { User } from './auth'
